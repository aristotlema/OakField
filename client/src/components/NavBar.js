import React from 'react';
import './NavBar.scss';

const NavBar = () => {
    return (
        <nav>
            <h2 className="main-logo">
                OakField
            </h2>
        </nav>
    );
};

export default NavBar;